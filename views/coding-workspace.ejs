<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= test.codingBank.title %> | Mr & Ms Coder Coding Round</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />

  <%- include('partials/tailwind-setup') %>
  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/codemirror.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/theme/dracula.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/codemirror.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/javascript/javascript.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/python/python.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.13/mode/clike/clike.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

</head>
<body class="vc-app h-screen overflow-hidden flex flex-col p-3 md:p-5 gap-4 relative selection:bg-white/30 text-white">

  <div class="absolute inset-0 grid-bg pointer-events-none z-0"></div>

  <div id="start-modal" class="fixed inset-0 bg-black/80 backdrop-blur-md z-[100] flex items-center justify-center p-6">
    <div class="glass-card rounded-2xl p-8 md:p-12 w-full max-w-md relative overflow-hidden">
      <div class="absolute top-0 right-0 w-32 h-32 bg-blue-500/10 rounded-full blur-3xl pointer-events-none"></div>

      <div class="text-center mb-10 relative z-10">
        <div class="mx-auto w-12 h-12 border border-white/20 bg-white/5 rounded-xl flex items-center justify-center mb-6 shadow-inner">
          <span class="material-symbols-outlined text-white text-[24px]">schedule</span>
        </div>
        <h1 class="text-2xl font-semibold text-white tracking-tight mb-2">Mr & Ms Coder Coding Round</h1>
        <p class="text-gray-400 font-light text-sm">Enter details to begin your Vansh2k26 attempt</p>
      </div>

      <div class="space-y-4 relative z-10">
        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">person</span>
          </div>
          <input id="start-name" type="text" placeholder="Student Name" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white placeholder-gray-600 focus:bg-[#111] focus:border-white/30 transition-all outline-none" required>
        </div>

        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">badge</span>
          </div>
          <input id="start-roll-no" type="text" placeholder="Roll Number" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white placeholder-gray-600 focus:bg-[#111] focus:border-white/30 transition-all outline-none" required>
        </div>

        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">apartment</span>
          </div>
          <select id="start-department" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white focus:bg-[#111] focus:border-white/30 transition-all outline-none appearance-none" required>
            <option value="">Select Department</option>
          <option value="CSE">CSE</option>
          <option value="CSE(AI&ML)">CSE(AI&amp;ML)</option>
          <option value="CSE(DS)">CSE(DS)</option>
          <option value="IT">IT</option>
          <option value="ECE">ECE</option>
          <option value="EEE">EEE</option>
          <option value="CIVIL">CIVIL</option>
          <option value="MECH">MECH</option>
          <option value="EIE">EIE</option>
          <option value="AI&DS">AI&amp;DS</option>
          <option value="AI&ML">AI&amp;ML</option>
          </select>
        </div>

        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">school</span>
          </div>
          <select id="start-year" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white focus:bg-[#111] focus:border-white/30 transition-all outline-none appearance-none" required>
            <option value="">Select Year</option>
          <option value="1">1st Year</option>
          <option value="2">2nd Year</option>
          <option value="3">3rd Year</option>
          <option value="4">4th Year</option>
          </select>
        </div>

        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">groups</span>
          </div>
          <input id="start-section" type="text" placeholder="Section (Optional)" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white placeholder-gray-600 focus:bg-[#111] focus:border-white/30 transition-all outline-none">
        </div>

        <div class="relative">
          <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-500 text-[18px]">school</span>
          </div>
          <input id="start-college" type="text" placeholder="College Name (leave blank if Vignan)" class="w-full pl-12 pr-4 py-3 bg-[#0a0a0a] border border-white/10 rounded-xl text-sm font-medium text-white placeholder-gray-600 focus:bg-[#111] focus:border-white/30 transition-all outline-none">
        </div>

        <button id="start-session-btn" class="w-full bg-white text-black hover:bg-gray-200 py-3 px-4 rounded-xl text-sm font-semibold transition-colors shadow-[0_0_15px_rgba(255,255,255,0.1)] active:scale-[0.98] flex justify-center items-center gap-2">
          Start Test <span class="material-symbols-outlined text-[18px]">arrow_forward</span>
        </button>
        <p id="start-modal-error" class="hidden text-red-400 text-xs text-center font-medium"></p>
      </div>
    </div>
  </div>

  <div id="security-warning-modal" class="hidden fixed inset-0 bg-black/80 backdrop-blur-md z-[100] items-center justify-center p-6">
    <div class="glass-card rounded-2xl shadow-2xl p-8 md:p-10 w-full max-w-md text-center border border-red-500/30">
      <div class="w-16 h-16 bg-red-500/10 border border-red-500/20 text-red-400 rounded-full flex items-center justify-center mx-auto mb-6">
        <span class="material-symbols-outlined text-[32px]">security</span>
      </div>

      <h2 class="text-2xl font-semibold text-white tracking-tight mb-2">Warning!</h2>
      <p class="text-sm font-light text-gray-400 mb-6 leading-relaxed">You left fullscreen or switched tabs.</p>

      <div class="bg-red-500/10 border border-red-500/20 p-4 rounded-xl mb-8">
        <p class="text-[11px] text-red-400 uppercase tracking-widest leading-relaxed">If this happens again, your coding session will be automatically submitted.</p>
      </div>

      <div class="flex flex-col sm:flex-row gap-3">
        <button id="return-to-coding" class="flex-1 bg-white text-black hover:bg-gray-200 py-3 px-4 rounded-xl text-sm font-semibold transition-colors">
          Return to Session
        </button>
        <button id="submit-coding-now" class="w-full sm:w-max bg-transparent text-gray-500 hover:text-white border border-transparent hover:border-white/10 py-3 px-4 rounded-xl text-sm transition-colors">
          Submit Now
        </button>
      </div>
    </div>
  </div>

  <!-- Top Nav Pill -->
  <nav class="glass-nav rounded-2xl h-16 md:h-20 flex items-center justify-between px-6 shrink-0 z-10 shadow-lg relative overflow-hidden">
    <div class="absolute top-0 right-1/2 w-48 h-10 bg-blue-500/10 rounded-full blur-3xl pointer-events-none"></div>

    <div class="flex items-center gap-4 relative z-10">
      <div class="border border-white/10 bg-white/5 w-10 h-10 rounded-xl flex items-center justify-center">
        <span class="material-symbols-outlined text-white text-[20px]">terminal</span>
      </div>
      <div>
        <h1 class="font-semibold text-sm md:text-base tracking-tight leading-none text-white"><%= test.codingBank.title %></h1>
        <p id="student-session-meta" class="text-gray-500 text-[10px] font-mono uppercase tracking-widest hidden sm:block mt-1">Vansh2k26 Coding Arena</p>
      </div>
    </div>
      <div class="flex items-center gap-4 relative z-10">
        <div id="session-timer" class="bg-[#0a0a0a] text-white border border-white/10 rounded-lg px-4 py-2 text-sm font-mono tracking-widest min-w-[88px] text-center">
          --:--
        </div>
        <input type="text" id="roll-no" placeholder="Roll Number" class="bg-[#0a0a0a] text-white placeholder-gray-600 border border-white/10 rounded-lg px-4 py-2 text-sm font-medium outline-none transition-all uppercase tracking-widest text-center w-36 md:w-48 shadow-inner" readonly>
        <button id="final-submit-btn" class="bg-white text-black hover:bg-gray-200 px-6 py-2 rounded-lg text-sm font-semibold shadow-[0_0_15px_rgba(255,255,255,0.1)] transition-transform active:scale-[0.98] flex items-center gap-2">
          <span class="material-symbols-outlined text-[18px]">publish</span> <span class="hidden sm:inline">Submit Final Test</span>
      </button>
    </div>
  </nav>

  <!-- Complete Workspace Split Panes -->
  <div class="flex-1 flex flex-col md:flex-row gap-4 overflow-hidden z-10 relative">
    
    <!-- LEFT MOST: Question Navigator -->
    <div class="w-full h-24 md:h-full md:w-20 lg:w-64 glass-card rounded-2xl flex flex-col shrink-0 overflow-hidden shadow-2xl">
      <div class="px-5 py-4 border-b border-white/10 bg-[#050505] shrink-0 hidden lg:flex items-center justify-between">
        <h3 class="text-[10px] font-semibold text-gray-400 uppercase tracking-widest">Question Bank</h3>
        <span class="material-symbols-outlined text-[16px] text-gray-500">format_list_bulleted</span>
      </div>
      <div class="flex-1 overflow-x-auto md:overflow-x-hidden md:overflow-y-auto p-3 flex md:flex-col gap-2" id="question-tabs">
        <% test.codingBank.challenges.forEach((c, index) => { %>
          <button data-id="<%= c._id %>" class="challenge-tab shrink-0 md:shrink w-auto md:w-full text-left p-3 rounded-xl border border-transparent hover:bg-white/5 transition-all flex items-center gap-3 group">
            <div class="w-8 h-8 rounded-lg shrink-0 bg-[#0a0a0a] border border-white/10 group-[.active]:bg-white group-[.active]:text-black text-gray-400 flex items-center justify-center font-mono text-sm transition-colors">
              <%= index + 1 %>
            </div>
            <div class="hidden lg:block truncate">
              <div class="text-sm font-medium text-gray-300 group-[.active]:text-white truncate mb-0.5"><%= c.title %></div>
              <div class="text-[10px] font-mono uppercase tracking-widest text-gray-500 group-[.active]:text-gray-400"><%= c.difficulty %></div>
            </div>
          </button>
        <% }); %>
      </div>
    </div>

    <!-- MIDDLE: Problem Description -->
    <div class="hidden md:flex flex-col w-1/3 xl:w-[35%] glass-card rounded-2xl border border-white/10 overflow-hidden shrink-0 shadow-2xl bg-[#0a0a0a]">
      <div class="px-6 py-5 border-b border-white/10 bg-[#050505] shrink-0">
        <div class="flex items-center gap-3 mb-2">
          <span id="current-q-diff" class="text-[10px] px-2 py-0.5 border rounded uppercase tracking-widest font-mono">--</span>
        </div>
        <h2 id="current-q-title" class="text-xl font-semibold text-white tracking-tight">Loading...</h2>
      </div>
      <div id="current-q-desc" class="p-6 md:p-8 overflow-y-auto flex-1 prose prose-invert prose-sm max-w-none text-gray-300 font-normal leading-relaxed">
        <!-- Markdown -->
      </div>
    </div>

    <!-- RIGHT: Code Editor & Console -->
    <div class="flex-1 flex flex-col bg-[#050505] rounded-2xl border border-white/10 overflow-hidden relative shadow-2xl">
      <div class="h-14 xl:h-16 bg-[#0a0a0a] border-b border-white/10 flex items-center justify-between px-4 shrink-0">
        <div class="flex items-center gap-2">
          <span class="material-symbols-outlined text-[16px] text-gray-500">code</span>
          <select id="language-select" class="bg-transparent text-gray-300 text-xs font-mono outline-none hover:text-white transition-colors cursor-pointer appearance-none border border-transparent hover:border-white/10 px-2 py-1 rounded">
            <option value="javascript" class="bg-[#111]">JavaScript (Node.js)</option>
            <option value="python" class="bg-[#111]">Python 3.10</option>
            <option value="java" class="bg-[#111]">Java 15</option>
            <option value="cpp" class="bg-[#111]">C++ (GCC 12)</option>
            <option value="c" class="bg-[#111]">C (GCC 12)</option>
          </select>
        </div>
        <button id="run-btn" class="flex items-center gap-2 text-white bg-white/5 hover:bg-white/10 border border-white/10 px-4 py-1.5 rounded-lg text-xs font-medium transition-all active:scale-[0.98]">
          <span class="material-symbols-outlined text-[16px]">play_arrow</span> Run <span class="hidden xl:inline">Code</span>
        </button>
      </div>

      <!-- Editor -->
      <div class="flex-1 relative p-0" id="editor-container"></div>

      <!-- Console Drawer -->
      <div id="console-drawer" class="h-[45px] bg-[#0a0a0a] border-t border-white/10 flex flex-col shrink-0 transition-all duration-300 z-10 relative">
        <div class="flex items-center justify-between px-6 h-[45px] cursor-pointer select-none group hover:bg-[#111] transition-colors" id="console-toggle">
          <h3 class="text-gray-400 hover:text-white text-xs font-semibold flex items-center gap-2 uppercase tracking-widest transition-colors">
            <span class="material-symbols-outlined text-[16px]">terminal</span> Execution Console
          </h3>
          <div class="flex items-center gap-4">
            <span id="status-badge" class="hidden text-[10px] px-2 py-0.5 border rounded uppercase tracking-widest font-mono"></span>
            <span class="material-symbols-outlined text-[16px] text-gray-500 group-hover:text-white transition-colors" id="console-icon">expand_less</span>
          </div>
        </div>
        <div class="flex-1 overflow-y-auto p-4 md:p-6 text-[13px] font-mono whitespace-pre-wrap bg-[#050505] leading-relaxed text-gray-300 border-t border-white/5" id="console-output">
          <span class="text-gray-600">Waiting for execution...</span>
        </div>
      </div>
    </div>
  </div>

  <script id="server-data" type="application/json">
    <%- JSON.stringify({
      testId: test._id,
      uniqueLink: test.uniqueLink,
      challenges: test.codingBank.challenges.map(c => ({
        id: c._id, title: c.title, difficulty: c.difficulty, 
        description: c.description,
        sampleTest: c.testCases.find(tc => !tc.isHidden) || null
      }))
    }) %>
  </script>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const data = JSON.parse(document.getElementById('server-data').textContent);
      const testId = data.testId;
      const uniqueLink = data.uniqueLink;
      const challenges = data.challenges;
      const sessionDurationMinutes = <%= Number(test.durationMinutes || 30) %>;

      const submissions = {};
      challenges.forEach(c => {
        submissions[c.id] = { code: '// Enter entry-point logic here...\n', language: 'javascript' };
      });

      let activeChallengeId = challenges[0].id;

      const editor = CodeMirror(document.getElementById('editor-container'), {
        lineNumbers: true, theme: 'dracula', mode: 'javascript', indentUnit: 4, tabSize: 4, lineWrapping: true
      });

      const langSelect = document.getElementById('language-select');
      const currentTitle = document.getElementById('current-q-title');
      const currentDiff = document.getElementById('current-q-diff');
      const currentDesc = document.getElementById('current-q-desc');
      const tabs = document.querySelectorAll('.challenge-tab');
      
      const consoleDrawer = document.getElementById('console-drawer');
      const consoleToggle = document.getElementById('console-toggle');
      const consoleIcon = document.getElementById('console-icon');
      const statusBadge = document.getElementById('status-badge');
      const outputDiv = document.getElementById('console-output');
      const sessionTimerEl = document.getElementById('session-timer');
      const studentSessionMeta = document.getElementById('student-session-meta');
      const rollNoInput = document.getElementById('roll-no');
      const startModal = document.getElementById('start-modal');
      const startNameInput = document.getElementById('start-name');
      const startRollNoInput = document.getElementById('start-roll-no');
      const startDepartmentInput = document.getElementById('start-department');
      const startYearInput = document.getElementById('start-year');
      const startSectionInput = document.getElementById('start-section');
      const startCollegeInput = document.getElementById('start-college');
      const startSessionBtn = document.getElementById('start-session-btn');
      const startModalError = document.getElementById('start-modal-error');
      const runBtn = document.getElementById('run-btn');
      const finalSubmitBtn = document.getElementById('final-submit-btn');
      const securityWarningModal = document.getElementById('security-warning-modal');
      const returnToCodingBtn = document.getElementById('return-to-coding');
      const submitCodingNowBtn = document.getElementById('submit-coding-now');
      let sessionTimeLeftSeconds = Math.max(1, sessionDurationMinutes) * 60;
      let sessionTimerId = null;
      let hasSubmitted = false;
      let hasStarted = false;
      let warningCount = 0;
      let studentProfile = null;
      let isRunningCode = false;
      let isSubmittingFinal = false;

      function setBusyState(button, isBusy, idleMarkup, busyMarkup) {
        button.disabled = isBusy;
        button.classList.toggle('opacity-70', isBusy);
        button.classList.toggle('cursor-not-allowed', isBusy);
        button.innerHTML = isBusy ? busyMarkup : idleMarkup;
      }

      function loadChallenge(id) {
        submissions[activeChallengeId].code = editor.getValue();
        submissions[activeChallengeId].language = langSelect.value;

        activeChallengeId = id;
        const chal = challenges.find(c => c.id === id);
        
        tabs.forEach(t => t.classList.toggle('active', t.dataset.id === id));
        currentTitle.textContent = chal.title;
        currentDiff.textContent = chal.difficulty;

        let diffClass = 'border-gray-500/30 text-gray-400 bg-gray-500/10';
        if(chal.difficulty === 'Easy') diffClass = 'border-green-500/30 text-green-400 bg-green-500/10';
        if(chal.difficulty === 'Medium') diffClass = 'border-orange-500/30 text-orange-400 bg-orange-500/10';
        if(chal.difficulty === 'Hard') diffClass = 'border-red-500/30 text-red-400 bg-red-500/10';
        
        currentDiff.className = `text-[10px] px-2 py-0.5 border rounded uppercase tracking-widest font-mono ${diffClass}`;
        
        currentDesc.innerHTML = marked.parse(chal.description);

        langSelect.value = submissions[id].language;
        updateCodeMirrorMode(submissions[id].language);
        editor.setValue(submissions[id].code);

        if(consoleIcon.textContent === 'expand_more') consoleToggle.click();
        statusBadge.classList.add('hidden');
        outputDiv.innerHTML = '<span class="text-gray-600">Awaiting execution trace...</span>';
      }

      function updateCodeMirrorMode(lang) {
        let mode = 'javascript';
        if (lang === 'python') mode = 'python';
        if (['cpp', 'c', 'java'].includes(lang)) mode = 'text/x-c++src';
        editor.setOption('mode', mode);
      }

      function renderSessionTimer() {
        const minutes = Math.floor(sessionTimeLeftSeconds / 60);
        const seconds = sessionTimeLeftSeconds % 60;
        sessionTimerEl.textContent = `${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
      }

      function startSessionTimer() {
        renderSessionTimer();
        sessionTimerId = setInterval(() => {
          if (hasSubmitted) {
            clearInterval(sessionTimerId);
            return;
          }

          sessionTimeLeftSeconds -= 1;
          if (sessionTimeLeftSeconds <= 0) {
            clearInterval(sessionTimerId);
            sessionTimeLeftSeconds = 0;
            renderSessionTimer();
            submitFinal(true);
            return;
          }

          renderSessionTimer();
        }, 1000);
      }

      function lockWorkspaceUntilStart() {
        tabs.forEach((tab) => {
          tab.disabled = true;
          tab.classList.add('opacity-50', 'cursor-not-allowed');
        });
        langSelect.disabled = true;
        runBtn.disabled = true;
        finalSubmitBtn.disabled = true;
        rollNoInput.readOnly = true;
        editor.setOption('readOnly', 'nocursor');
        sessionTimerEl.textContent = '--:--';
      }

      function unlockWorkspaceAfterStart() {
        tabs.forEach((tab) => {
          tab.disabled = false;
          tab.classList.remove('opacity-50', 'cursor-not-allowed');
        });
        langSelect.disabled = false;
        runBtn.disabled = false;
        finalSubmitBtn.disabled = false;
        editor.setOption('readOnly', false);
      }

      function addSecurityListeners() {
        window.addEventListener('beforeunload', (event) => {
          if (!hasSubmitted && hasStarted) {
            event.preventDefault();
            event.returnValue = '';
          }
        });

        document.addEventListener('visibilitychange', () => {
          if (hasStarted && !hasSubmitted && document.hidden) {
            handleWarning();
          }
        });

        document.addEventListener('fullscreenchange', () => {
          if (hasStarted && !hasSubmitted && !document.fullscreenElement) {
            handleWarning();
            document.documentElement.requestFullscreen().catch(() => {});
          }
        });
      }

      function handleWarning() {
        if (hasSubmitted) return;
        warningCount += 1;

        if (warningCount === 1) {
          securityWarningModal.classList.remove('hidden');
          securityWarningModal.classList.add('flex');
          return;
        }

        submitFinal(true);
      }

      langSelect.addEventListener('change', (e) => updateCodeMirrorMode(e.target.value));
      tabs.forEach(t => t.addEventListener('click', () => loadChallenge(t.dataset.id)));
      loadChallenge(activeChallengeId);
      lockWorkspaceUntilStart();

      startSessionBtn.addEventListener('click', async () => {
        if (startSessionBtn.disabled) {
          return;
        }

        const enteredName = startNameInput.value.trim();
        const enteredRollNo = startRollNoInput.value.trim();
        const enteredDepartment = startDepartmentInput.value.trim();
        const enteredYear = startYearInput.value.trim();
        const enteredSection = startSectionInput.value.trim();
        const enteredCollege = startCollegeInput.value.trim();

        if (!enteredName || !enteredRollNo || !enteredDepartment || !enteredYear) {
          startModalError.textContent = 'Name, roll number, department, and year are required to start.';
          startModalError.classList.remove('hidden');
          return;
        }

        startSessionBtn.disabled = true;
        startModalError.classList.add('hidden');
        const startIdleMarkup = 'Start Now';
        startSessionBtn.innerHTML = '<span class="material-symbols-outlined animate-spin text-[18px] align-middle">sync</span> Please wait...';

        try {
          const response = await fetch('/api/coding/student/start', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              uniqueLink,
              name: enteredName,
              rollNo: enteredRollNo,
              department: enteredDepartment,
              year: enteredYear,
              section: enteredSection,
              collegeName: enteredCollege,
            }),
          });
          const result = await response.json();

          if (!response.ok) {
            startModalError.textContent = result.message || 'Unable to start coding test.';
            startModalError.classList.remove('hidden');
            return;
          }

          studentProfile = result.student;
          hasStarted = true;
          rollNoInput.value = studentProfile.rollNo;
          studentSessionMeta.textContent = `${studentProfile.name} • ${studentProfile.department} • ${studentProfile.year}${studentProfile.section ? ` • ${studentProfile.section}` : ''}`;
          startModal.classList.add('hidden');
          unlockWorkspaceAfterStart();
          document.documentElement.requestFullscreen().catch(() => {});
          addSecurityListeners();
          startSessionTimer();
        } catch (error) {
          startModalError.textContent = 'A network error occurred while starting the coding test.';
          startModalError.classList.remove('hidden');
        } finally {
          startSessionBtn.disabled = false;
          startSessionBtn.innerHTML = startIdleMarkup;
        }
      });

      returnToCodingBtn.addEventListener('click', () => {
        securityWarningModal.classList.add('hidden');
        securityWarningModal.classList.remove('flex');
        document.documentElement.requestFullscreen().catch(() => {});
      });

      submitCodingNowBtn.addEventListener('click', async () => {
        securityWarningModal.classList.add('hidden');
        securityWarningModal.classList.remove('flex');
        await submitFinal(true);
      });

      [startNameInput, startRollNoInput, startDepartmentInput, startYearInput, startSectionInput, startCollegeInput].forEach((input) => {
        input.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') {
          e.preventDefault();
          startSessionBtn.click();
        }
        });
      });

      consoleToggle.addEventListener('click', () => {
        if (consoleDrawer.style.height === '40%') {
          consoleDrawer.style.height = '45px'; consoleIcon.textContent = 'expand_less';
        } else {
          consoleDrawer.style.height = '40%'; consoleIcon.textContent = 'expand_more';
        }
      });
      function forceOpenConsole() { if(consoleDrawer.style.height !== '40%') consoleToggle.click(); }
      function setStatus(msg, type) {
        statusBadge.textContent = msg;
        statusBadge.classList.remove('hidden');
        statusBadge.className = `text-[10px] px-2 py-0.5 border rounded uppercase tracking-widest font-mono ${type==='err'?'border-red-500/30 bg-red-500/10 text-red-400':(type==='ok'?'border-green-500/30 bg-green-500/10 text-green-400':'border-white/30 bg-white/10 text-white')}`;
      }

      document.getElementById('run-btn').addEventListener('click', async () => {
        if (!hasStarted || isRunningCode || isSubmittingFinal) return;
        isRunningCode = true;
        forceOpenConsole();
        outputDiv.innerHTML = '<span class="text-white animate-pulse font-mono tracking-widest">Executing code...</span>';
        setStatus('Running', 'warn');
        setBusyState(
          runBtn,
          true,
          '<span class="material-symbols-outlined text-[16px]">play_arrow</span> Run <span class="hidden xl:inline">Code</span>',
          '<span class="material-symbols-outlined animate-spin text-[16px]">sync</span> Please wait',
        );

        const code = editor.getValue();
        const chal = challenges.find(c => c.id === activeChallengeId);
        const input = chal.sampleTest ? chal.sampleTest.input : '';

        try {
          const res = await fetch('/api/coding/student/execute', {
            method: 'POST', headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ code, language: langSelect.value, input })
          });
          const data = await res.json();

          if(data.status === 'Compilation Error') {
            outputDiv.innerHTML = `<span class="text-red-400"><strong>Compilation Error:</strong>\n${escapeHtml(data.output)}</span>`;
            setStatus('Error', 'err');
          } else {
             let expected = chal.sampleTest ? chal.sampleTest.expectedOutput.trim() : '';
             let got = (data.output || '').trim();
             let out = `<strong class="text-gray-400">Standard Output:</strong>\n<span class="text-white">${escapeHtml(got || '(No Output)')}</span>\n\n`;
             if(chal.sampleTest) {
               if(expected === got) {
                 out += `<span class="text-green-400 font-mono text-xs block mt-2 border border-green-500/20 bg-green-500/10 px-3 py-2 rounded">✓ Passed I/O Match</span>`;
                 setStatus('Passed', 'ok');
               } else {
                 out += `<span class="text-red-400 mt-2 block font-mono text-xs border border-red-500/20 bg-red-500/10 px-3 py-2 rounded">✗ Output Mismatch\n<span class="text-gray-500 mt-1 block">Expected Control: ${escapeHtml(expected)}</span></span>`;
                 setStatus('Failed', 'err');
               }
             } else {
                 setStatus('Success', 'ok');
             }
             outputDiv.innerHTML = out;
          }
        } catch(e) {
          outputDiv.innerHTML = `<span class="text-red-400">Network Trace Error: ${e.message}</span>`;
          setStatus('Error', 'err');
        } finally {
          isRunningCode = false;
          setBusyState(
            runBtn,
            false,
            '<span class="material-symbols-outlined text-[16px]">play_arrow</span> Run <span class="hidden xl:inline">Code</span>',
            '',
          );
        }
      });

      document.getElementById('final-submit-btn').addEventListener('click', async () => {
        await submitFinal(false);
      });

      async function submitFinal(isAutoSubmit) {
        if (!hasStarted) {
          return;
        }

        if (hasSubmitted || isSubmittingFinal) return;
        if (!studentProfile || !studentProfile.rollNo) {
          forceOpenConsole();
          outputDiv.innerHTML = '<span class="text-red-400">Student details are missing. Please start the coding session again.</span>';
          setStatus('Error', 'err');
          return;
        }
        if(!isAutoSubmit && !confirm('Are you absolutely certain? This will lock your answers and submit the test.')) return;

        hasSubmitted = true;
        isSubmittingFinal = true;
        if (sessionTimerId) {
          clearInterval(sessionTimerId);
        }

        submissions[activeChallengeId].code = editor.getValue();
        submissions[activeChallengeId].language = langSelect.value;
        
        forceOpenConsole();
        outputDiv.innerHTML = '<span class="text-white animate-pulse font-mono tracking-widest">Grading submission against hidden server test cases...</span>';
        setStatus('Commit', 'warn');
        setBusyState(
          finalSubmitBtn,
          true,
          '<span class="material-symbols-outlined text-[18px]">publish</span> <span class="hidden sm:inline">Submit Final Test</span>',
          '<span class="material-symbols-outlined animate-spin text-[18px]">sync</span> <span class="hidden sm:inline">Submitting...</span>',
        );
        runBtn.disabled = true;

        const payload = Object.keys(submissions).map(id => ({
          challengeId: id, code: submissions[id].code, language: submissions[id].language
        }));

        try {
          const res = await fetch('/api/coding/student/submit', {
            method: 'POST', headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              studentName: studentProfile.name,
              studentRollNo: studentProfile.rollNo,
              studentDepartment: studentProfile.department,
              studentYear: studentProfile.year,
              studentSection: studentProfile.section,
              studentCollege: studentProfile.collegeName,
              testId: testId,
              submissions: payload,
            })
          });
          
          if(res.ok) {
            if (document.fullscreenElement) {
              document.exitFullscreen().catch(() => {});
            }
            outputDiv.innerHTML = `<span class="text-green-400 font-medium block mb-2">Test Successfully Archived! 🎉</span><div class="text-gray-500 text-xs">Your final score has been logged. You may close this window.</div>`;
            setStatus('Archived', 'ok');
            setTimeout(() => { document.body.innerHTML = `<div class="h-screen w-full flex items-center justify-center bg-[#000] relative px-6"><div class="absolute inset-0 grid-bg pointer-events-none z-0"></div><div class="w-full max-w-lg text-center p-10 md:p-12 bg-[#0a0a0a] shadow-2xl rounded-3xl border border-white/10 relative z-10"><div class="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-2xl border border-white/10 bg-white/5"><span class="material-symbols-outlined text-white text-[34px] leading-none">task_alt</span></div><h1 class="text-3xl font-semibold text-white tracking-tight">Assessment Complete</h1><p class="mt-4 text-gray-400 text-sm leading-relaxed">Your coding test has been submitted successfully.</p><p class="text-gray-500 font-mono text-xs uppercase tracking-[0.35em] mt-5">Safe to exit</p></div></div>`; }, 3000);
          } else {
            const data = await res.json();
            outputDiv.innerHTML = `<span class="text-red-400">${data.message || 'System fault during commit.'}</span>`;
            setStatus('Error', 'err');
            hasSubmitted = false;
            isSubmittingFinal = false;
          }
        } catch(e) {
          outputDiv.innerHTML = `<span class="text-red-400">Network fault: ${e.message}</span>`;
          setStatus('Error', 'err');
          hasSubmitted = false;
          isSubmittingFinal = false;
        } finally {
          if (!hasSubmitted) {
            setBusyState(
              finalSubmitBtn,
              false,
              '<span class="material-symbols-outlined text-[18px]">publish</span> <span class="hidden sm:inline">Submit Final Test</span>',
              '',
            );
            runBtn.disabled = false;
          }
        }
      }

      function escapeHtml(unsafe) { return unsafe.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;"); }
    });
  </script>
</body>
</html>
